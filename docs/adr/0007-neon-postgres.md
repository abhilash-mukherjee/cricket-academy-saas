# Neon hosts Postgres

The system of record is Postgres on Neon so we get a managed database and preview branching next to Vercel, without taking on Supabase Auth or storage. RDS or a VPS Postgres would make the solo operator the DBA.
