# Managed infra for a solo operator

For the next year, one person runs production and will not self-host. Vendors own Postgres, TLS, and deploys; we pay for that so product work is not blocked on ops.
